[build]
command = "echo 'Health Flash Report files already generated by GitHub Actions'"
publish = "."

# Skip processing since files are pre-built by GitHub Actions
[build.processing]
skip_processing = false

[build.environment]
PYTHON_VERSION = "3.13"

# Health Flash Report - Daily health news automation
# Files are generated by GitHub Actions workflow daily at 6 AM UTC 